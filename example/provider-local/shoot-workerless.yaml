apiVersion: core.gardener.cloud/v1beta1
kind: Shoot
metadata:
  name: local-wl
  namespace: garden-local
spec:
  controlPlane:
    highAvailability:
      failureTolerance:
        type: node # {node,zone}
  cloudProfile:
    name: local
  region: local
  provider:
    type: local
